<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.prodigal.system.mapper.PictureMapper">

    <resultMap id="BaseResultMap" type="com.prodigal.system.model.entity.Picture">
        <id property="id" column="id" jdbcType="VARCHAR"/>
        <result property="originUrl" column="origin_url" jdbcType="VARCHAR"/>
        <result property="url" column="url" jdbcType="VARCHAR"/>
        <result property="thumbnailUrl" column="thumbnail_url" jdbcType="VARCHAR"/>
        <result property="sourceUrl" column="source_url" jdbcType="VARCHAR"/>
        <result property="spaceId" column="space_id" jdbcType="VARCHAR"/>
        <result property="name" column="name" jdbcType="VARCHAR"/>
        <result property="introduction" column="introduction" jdbcType="VARCHAR"/>
        <result property="category" column="category" jdbcType="VARCHAR"/>
        <result property="tags" column="tags" jdbcType="VARCHAR"/>
        <result property="picColor" column="pic_color" jdbcType="VARCHAR"/>
        <result property="picSize" column="pic_size" jdbcType="BIGINT"/>
        <result property="picWidth" column="pic_width" jdbcType="INTEGER"/>
        <result property="picHeight" column="pic_height" jdbcType="INTEGER"/>
        <result property="picScale" column="pic_scale" jdbcType="DOUBLE"/>
        <result property="picFormat" column="pic_format" jdbcType="VARCHAR"/>
        <result property="userId" column="user_id" jdbcType="VARCHAR"/>
        <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
        <result property="editTime" column="edit_time" jdbcType="TIMESTAMP"/>
        <result property="updateTime" column="update_time" jdbcType="TIMESTAMP"/>
        <result property="isDelete" column="is_delete" jdbcType="TINYINT"/>
        <result property="reviewStatus" column="review_status" jdbcType="INTEGER"/>
        <result property="reviewMessage" column="review_message" jdbcType="VARCHAR"/>
        <result property="reviewerId" column="reviewer_id" jdbcType="VARCHAR"/>
        <result property="reviewTime" column="review_time" jdbcType="TIMESTAMP"/>
        <result property="viewQuantity" column="view_quantity" jdbcType="BIGINT"/>
        <result property="shareQuantity" column="share_quantity" jdbcType="BIGINT"/>
    </resultMap>

    <sql id="Base_Column_List">
        id,origin_url,url,thumbnail_url,
        source_url,space_id,name,
        introduction,category,tags,
        pic_color,pic_size,pic_width,pic_height,
        pic_scale,pic_format,user_id,
        create_time,edit_time,update_time,
        is_delete,review_status,review_message,
        reviewer_id,review_time,view_quantity,share_quantity
    </sql>

    <!--查询已删除且修改时间在指定时间之前的图片-->
    <select id="selectDeletedPictures" resultMap="BaseResultMap">
        select
            <include refid="Base_Column_List" />
        from picture where is_delete = 1 and space_id in
        <foreach collection="spaceIds" item="spaceId" open="(" separator="," close=")">
            #{spaceId}
        </foreach>
        and edit_time <![CDATA[<]]> #{date}
    </select>

    <!-- 根据pictureIds 和 spaceIds 删除图片 -->
    <delete id="deletePicturesByPictureIdsAndSpaceId">
        delete from picture where id in
        <foreach collection="pictureIds" item="pictureId" open="(" separator="," close=")">
            #{pictureId}
        </foreach>
        and space_id =  #{spaceId}
    </delete>

    <!-- 删除已删除且修改时间在指定时间之前的图片-->
    <delete id="deleteDeletedPictures">
        delete from picture where is_delete = 1 and space_id in
        <foreach collection="spaceIds" item="spaceId" open="(" separator="," close=")">
            #{spaceId}
        </foreach>
        and edit_time <![CDATA[<]]> #{date}
    </delete>
</mapper>
